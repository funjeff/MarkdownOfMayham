void scroll_ttc_dl_Cube_mesh_layer_1_vtx_2() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Cube_mesh_layer_1_vtx_2);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_001_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_001_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_002_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_002_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_003_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_003_mesh_layer_1_vtx_3);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_004_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_004_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_005_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_005_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_006_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_006_mesh_layer_1_vtx_3);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_007_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_007_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_008_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_008_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_009_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_009_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_010_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_010_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_011_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_011_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_012_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_012_mesh_layer_1_vtx_3);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_013_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_013_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_014_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_014_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_015_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_015_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_016_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_016_mesh_layer_1_vtx_3);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_017_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_017_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_018_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_018_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_019_mesh_layer_1_vtx_2() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_019_mesh_layer_1_vtx_2);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_020_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_020_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_021_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_021_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_022_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_022_mesh_layer_1_vtx_3);

	deltaY = (int)(1.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_023_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_023_mesh_layer_1_vtx_3);

	deltaY = (int)(1.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_023_mesh_layer_1_vtx_4() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_023_mesh_layer_1_vtx_4);

	deltaY = (int)(1.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Dash_Left_024_mesh_layer_1_vtx_2() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Dash_Left_024_mesh_layer_1_vtx_2);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_2() {
	int i = 0;
	int count = 12;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_2);

	deltaY = (int)(-0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_4() {
	int i = 0;
	int count = 20;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_4);

	deltaY = (int)(0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_5() {
	int i = 0;
	int count = 4;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_5);

	deltaY = (int)(-0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_6() {
	int i = 0;
	int count = 14;
	int width = 32 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_6);

	deltaX = (int)(0.5 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_7() {
	int i = 0;
	int count = 8;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_7);

	deltaY = (int)(-0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_8() {
	int i = 0;
	int count = 8;
	int width = 32 * 0x20;

	static int currentX = 0;
	int deltaX;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_8);

	deltaX = (int)(-0.5 * 0x20) % width;

	if (absi(currentX) > width) {
		deltaX -= (int)(absi(currentX) / width) * width * signum_positive(deltaX);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[0] += deltaX;
	}
	currentX += deltaX;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_10() {
	int i = 0;
	int count = 8;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_10);

	deltaY = (int)(-0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_mesh_layer_1_vtx_11() {
	int i = 0;
	int count = 4;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_mesh_layer_1_vtx_11);

	deltaY = (int)(-0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_1() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_001_mesh_layer_1_vtx_1);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_3() {
	int i = 0;
	int count = 24;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_001_mesh_layer_1_vtx_3);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_4() {
	int i = 0;
	int count = 4;
	int height = 64 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_001_mesh_layer_1_vtx_4);

	deltaY = (int)(2.0 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_7() {
	int i = 0;
	int count = 4;
	int height = 32 * 0x20;

	static int currentY = 0;
	int deltaY;
	Vtx *vertices = segmented_to_virtual(ttc_dl_Geometry_001_mesh_layer_1_vtx_7);

	deltaY = (int)(0.5 * 0x20) % height;

	if (absi(currentY) > height) {
		deltaY -= (int)(absi(currentY) / height) * height * signum_positive(deltaY);
	}

	for (i = 0; i < count; i++) {
		vertices[i].n.tc[1] += deltaY;
	}
	currentY += deltaY;
}

void scroll_ttc() {
	scroll_ttc_dl_Cube_mesh_layer_1_vtx_2();
	scroll_ttc_dl_Dash_Left_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_001_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_002_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_003_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_004_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_005_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_006_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_007_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_008_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_009_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_010_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_011_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_012_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_013_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_014_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_015_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_016_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_017_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_018_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_019_mesh_layer_1_vtx_2();
	scroll_ttc_dl_Dash_Left_020_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_021_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_022_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_023_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Dash_Left_023_mesh_layer_1_vtx_4();
	scroll_ttc_dl_Dash_Left_024_mesh_layer_1_vtx_2();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_2();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_4();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_5();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_6();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_7();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_8();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_10();
	scroll_ttc_dl_Geometry_mesh_layer_1_vtx_11();
	scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_1();
	scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_3();
	scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_4();
	scroll_ttc_dl_Geometry_001_mesh_layer_1_vtx_7();
}
